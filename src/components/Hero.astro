---
import { Play } from 'lucide-react';
---

<section class="relative min-h-screen flex flex-col items-center justify-center pt-20 pb-10 overflow-hidden bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-blue-900/20 via-main to-main">

  <div class="container mx-auto px-4 z-10 flex flex-col items-center text-center max-w-5xl pointer-events-none">
    
    <!-- Tagline Badge -->
    <div class="pointer-events-auto fade-up inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-blue-500/30 bg-blue-500/5 backdrop-blur-sm mb-8 animate-fade-in-up" style="animation-delay: 0.1s;">
      <span class="text-accent text-xs font-bold tracking-wider uppercase">🚀 30+ Years of IT Architecture Experience</span>
    </div>

    <!-- H1 Headline -->
    <h1 class="pointer-events-auto fade-up text-5xl md:text-7xl font-extrabold tracking-tighter mb-6 leading-[1.1]" style="animation-delay: 0.2s;">
      We Don't Just Sell Tools.<br />
      <span class="text-gradient">We Build Your AI Workforce.</span>
    </h1>

    <!-- Subheadline -->
    <p class="pointer-events-auto fade-up text-lg md:text-xl text-secondary max-w-2xl mb-10 leading-relaxed" style="animation-delay: 0.3s;">
      Automate sales, support, and operations with human-like Voice AI Agents and custom CRM pipelines. Engineered for businesses in <strong class="text-white">NZ, Australia, and the USA</strong>.
    </p>

    <!-- CTAs -->
    <div class="pointer-events-auto fade-up flex flex-col sm:flex-row gap-4 w-full justify-center mb-16" style="animation-delay: 0.5s;">
      <a href="/contact" class="btn-primary flex items-center justify-center gap-2">
        Build My System
      </a>
      <a href="/voice-ai" class="px-8 py-4 rounded-full border border-white/10 hover:bg-white/5 hover:border-white/30 transition-all font-semibold flex items-center justify-center gap-2">
        Explore the Software
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      </a>
    </div>

  </div>
</section>

<style>
  @keyframes beam {
    0% { transform: translateX(-100%) skewX(-12deg); }
    100% { transform: translateX(200%) skewX(-12deg); }
  }
  .animate-beam {
    animation: beam 2s infinite linear; 
  }
  .fade-up {
    @apply opacity-0 animate-[fade-up_0.8s_ease-out_forwards];
  }
  @keyframes fade-up {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
